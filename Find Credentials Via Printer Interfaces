Gaining Credentials via LDAP config on printers

---Always check printers for LDAP configuration that has been set up. If you find one with stored LDAP credentials set the hostname address as the compromised host with a listener set up ; address and set up responder, click test and the credentials should show up in plain-text via responder. 
